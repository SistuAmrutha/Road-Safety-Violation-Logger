# Example environment variables for Render (do NOT commit real secrets here)
# Copy this file into Render's Environment variables UI or create a local .env for testing.

# Database (MySQL) - replace <HOST> and credentials with your DB values
SPRING_DATASOURCE_URL=jdbc:mysql://<MYSQL_HOST>:3306/oopsdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
SPRING_DATASOURCE_USERNAME=oops
SPRING_DATASOURCE_PASSWORD=oops_pass

# Active Spring profile (use 'mysql' if relying on application-mysql.properties)
SPRING_PROFILES_ACTIVE=mysql

# If you want Spring to run data.sql on a managed DB (use with caution)
# Set to 'always' to force SQL init on non-embedded databases
SPRING_SQL_INIT_MODE=embedded

# JVM options (optional)
JAVA_TOOL_OPTIONS=-Xmx512m

# NOTE:
# - Do NOT commit actual secrets to the repository. Use Render's Environment UI to add
#   SPRING_DATASOURCE_PASSWORD as a secret.
# - Render provides the PORT env var automatically; the Dockerfile uses it.
# - If you prefer Postgres on Render, replace SPRING_DATASOURCE_URL and add the
#   Postgres driver to pom.xml (I can do that for you if needed).
